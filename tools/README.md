# Tools

One-off helper tools live here.

Examples:

- Report generation helpers.
- Dataset manifest utilities.
- rosbag inspection helpers.
- CSV or JSON analysis scripts.

Tools must keep generated outputs under `runtime/` unless a task explicitly documents a different safe path.
